let soma, num, total, media
    while(num >= 0);
    num = prompt("Digite um número: ");

    if(num >= 0){
        soma = parseInt(soma) + parseInt(num);
        total++
    }
        media = soma / total;
        alert("A soma de todos números é: " + soma + "A media de todos os números é: " + media + "O total dos números digitados é: " + total);